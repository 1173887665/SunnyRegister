# SunnyRegister Frontend

React + TypeScript + Vite frontend for SunnyRegister.

```bash
npm ci
npm run dev
npm run build
```

`npm run build` writes production assets to `../backend/static`. The Go backend embeds these assets into the production binary and Docker image.
