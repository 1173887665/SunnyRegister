# SunnyRegister 前端产品与界面设计规范

> 用途：本文档完整描述 SunnyRegister 当前前端的信息架构、业务能力、交互状态与视觉基线，供 Stitch 生成下一版 UI 设计。本文不是宣传文案，也不是要求机械复刻现状；重设计可以更新布局和视觉语言，但不得遗漏本文标记的业务功能、状态、批量操作与安全约束。

## 1. 产品概览

### 1.1 产品定位

SunnyRegister 是面向管理员和运维人员的 ChatGPT 账户批量注册与生命周期管理控制台。它将邮箱池、注册或登录自动化、邮箱验证码、手机号接码、代理出口、OAuth 凭证、0 元试用检测、账户测活、AT 续期、Sub2API 反代导入和审计日志整合在一个后台中。

这是一款高频、长流程、强状态的运营工具。设计优先级依次为：

1. 快速识别账户和资源状态。
2. 安全地执行单条或批量操作。
3. 清晰观察长任务的进度、日志、异常与恢复状态。
4. 高效筛选、比较、复制和维护大量结构化数据。
5. 降低敏感凭证误展示、误修改和误删除的风险。

### 1.2 目标用户

- 维护邮箱、手机号和代理资源的系统管理员。
- 批量执行账户注册、接码和反代导入的运营人员。
- 排查自动化流程、供应商和上游接口问题的技术人员。
- 审核后台行为与导出记录的审计人员。

用户主要在桌面端工作，常见行为是长时间停留、跨页筛选、批量选择、查看日志和重复执行维护命令。移动端以监控、查询和少量单条操作为主。

### 1.3 技术与产品边界

- React 19、TypeScript、Vite、Tailwind CSS 4。
- Lucide 图标；不绘制同义自定义 SVG。
- React Router 路由。
- 中英文双语，所有布局必须容纳较长英文文案。
- 浅色与深色主题。
- GSAP 用于克制的页面进入和导航反馈，并支持 `prefers-reduced-motion`。
- 鉴权可关闭；关闭时直接进入管理端，开启时先显示登录前页面。

## 2. 信息架构

| 路由 | 菜单 | 核心任务 |
| --- | --- | --- |
| `/` | 工作台 | 选择邮箱、配置自动注册阶段、运行并观察批量任务 |
| `/mailbox` | 邮箱配置 | 维护邮箱池、分组、凭证、收件渠道与邮件查询 |
| `/phone` | 接码配置 | 配置外部接码供应商和维护自建手机号池 |
| `/sub2api` | 反代配置 | 配置 Sub2API 地址、分组、代理和导入参数 |
| `/proxy` | 代理配置 | 维护注册流量代理池并检测可用性 |
| `/session` | 账户管理 | 管理注册账户、Token、试用、测活、续期和反代导入 |
| `/audit` | 日志管理 | 筛选、查看、导出和清理审计日志 |

推荐继续保持七个一级菜单，不将资源配置和账户运维混入同一页面。可在导航中用“任务”“资源”“账户与审计”进行视觉分组，但不要增加用户必须展开的深层菜单。

## 3. 全局应用框架

### 3.1 管理端顶部导航

- 顶部为吸顶导航，桌面端三段布局：左侧品牌、中间一级菜单、右侧全局工具。
- 品牌必须首屏可见：`SunnyRegister`，可配小号产品副标题。
- 中间显示七个菜单，当前项需同时通过颜色、字重和背景或指示条表达，不能只靠颜色。
- 右侧为浅色/深色切换、中文/英文切换和退出。图标按钮应有 Tooltip 与可访问名称。
- 页面内容使用接近全宽的 `app-shell`，仅保留 12-28px 视口边距，以承载宽表格；不要将后台限制在窄居中列。
- 小屏导航应折叠为抽屉或菜单面板，并持续显示当前页面标题和核心全局工具。

### 3.2 页面标准结构

管理页面优先采用下列顺序，不要求每页都出现全部区域：

1. 紧凑页头：标题、1-2 行说明、页面级开关或主操作。
2. 统计或健康摘要：仅在确实帮助判断资源状态时出现。
3. 筛选与命令工具栏：筛选在左，刷新、导入、批量命令在右。
4. 主数据表或配置表单。
5. 分页、任务进度或辅助日志。

页面不是营销落地页。除登录前页面外，不使用巨型 Hero、装饰性大标题、浮动插画或大量留白。页面区块应使用无框分区、工具栏和表格面板，避免卡片套卡片。

### 3.3 全局反馈

- 成功和失败操作使用顶部 Toast；内容必须说明对象和结果，失败尽量包含可定位原因。
- 列表刷新时使用覆盖表格区域的 loading 层，保留页面上下文，避免整页闪白。
- 长任务显示持续进度，离开页面后可恢复；不能只显示短暂 Toast。
- 删除使用就近的非阻塞确认气泡，明确对象与不可撤销性。大范围批量删除可升级为模态确认。
- 空状态包含图标、简短原因和可执行下一步；加载、无结果和系统中尚无数据是三种不同状态。
- 操作按钮在运行时显示 Spinner、运行中文案并禁用重复提交。

### 3.4 敏感信息

- 密码、API Key、Admin Token、AT、RT、SK、TOTP 和收码 URL 均按敏感信息处理。
- 列表接口只展示“已配置/未配置”或缩写按钮，不直接铺开值。
- AT、RT、SK 按需请求后复制或在受控弹窗中查看；默认不长期显示明文。
- 编辑凭证采用“留空保持、非空替换、显式清除”语义，必须用清晰控件区分，不能让空输入无意清除已有凭证。
- 日志和错误详情中的敏感值必须脱敏。

## 4. 登录前页面

登录前页面是唯一允许更具品牌表达的区域，但第一目标仍是进入工具，而不是营销转化。

### 4.1 内容结构

- 顶部：品牌、GitHub 链接、语言切换、主题切换、登录按钮。
- Hero：产品名 `SunnyRegister` 作为主标题，辅助文案解释账户自动化与资源管理能力。
- 可视化运行控制台：模拟任务行、状态、统计和流程，传达产品真实工作方式。
- 六项核心能力：邮箱池、注册自动化、手机号接码、代理、反代导入、Session 管理。
- 四步流程：邮箱验证、注册/登录、手机号绑定、反代导入。
- 页脚：项目和外部链接。

### 4.2 登录抽屉

- 从右侧打开，桌面端保持背景页面可辨识，移动端可全屏。
- 字段：用户名、密码；支持回车提交。
- 状态：默认、字段校验、凭证错误、HTTP 429 限流、网络错误、提交中、登录成功。
- 可通过关闭按钮、遮罩和 Esc 退出；提交中防止重复提交。
- 退出登录后回到此页并展示成功 Toast。

## 5. 工作台

工作台是注册任务的启动和观察中心，不是通用数据总览。

### 5.1 账户选择区

- 筛选：邮箱搜索、邮箱分组、状态、套餐类型、状态更新时间排序。
- 表格列：选择、邮箱、所属分组、状态、套餐类型、状态更新时间、操作。
- 支持选择当前页、选择全部筛选结果、清除选择和跨页保留选择。
- 行操作为注册/登录相关的状态刷新；批量主操作为“自动注册”。
- 分页展示总数、当前范围、页码和每页数量。

### 5.2 自动注册弹窗

弹窗应采用清晰的分步配置或纵向分区，持续显示所选账户数量和依赖资源状态。

1. 注册身份：系统邮箱可用；Google、Microsoft 当前为预留或禁用状态，必须说明不可用原因。
2. 执行方式：协议模式、后台无头浏览器、可见浏览器。显示每种模式适用场景。
3. 协议挑战策略：原生无头浏览器接管、Sentinel 窄范围浏览器证明；仅在协议模式下显示。
4. 目标阶段：仅注册 ChatGPT、Codex 手机号绑定、导入反代平台、Agent Identity 绕过接码导入反代。
5. 并发数：数值输入或步进器，需显示有效范围。
6. 全流量代理池：二元开关。
7. 资源检查：邮箱池、接码配置、Sub2API 配置分别显示可用/不可用；缺少前置条件时禁用相关阶段并提供去配置入口。
8. 所选账户：紧凑可滚动列表，显示数量和是否已有 RT。

启动前必须让用户明确看到“将处理多少账户、使用何种模式、执行到哪个阶段”。执行中允许停止整批任务，停止操作需覆盖提交、排队、Worker、浏览器与邮箱读取阶段。

### 5.3 任务观察区

- 任务状态需在刷新或短暂断线后恢复，日志通过 SSE 或轮询持续更新。
- 展示“全局任务”和“当前账户”两个观察面板；每个面板有“进度/日志”标签。
- 进度视图：总数、完成、异常、待处理、当前步骤、百分比进度条。
- 日志视图：时间、邮箱、模块、级别、消息；等宽字体，信息密度高，可自动滚动但允许用户暂停阅读。
- 标准阶段包括：排队、初始化邮箱、代理就绪、浏览器或协议启动、提交邮箱、邮箱验证、认证完成、保存 Session、开始接码、收到手机验证码、完成绑定、反代导入、Agent Identity 导入、阶段未完成、取消、失败。
- 成功、部分完成、失败和取消必须有不同视觉语义；“注册成功但后续阶段失败”不能误画为完全失败或完全成功。

## 6. 邮箱配置

### 6.1 页面总览

- 页面级邮箱池总开关，表达“是否允许注册任务分配自建邮箱”，配置记录仍可编辑。
- 统计：总数、待注册、已注册、已接码、已反代、封禁、待二验、登录刷新、失败。统计项可作为快捷状态筛选。
- 筛选：邮箱搜索、分组、状态。
- 命令：选择全部筛选结果、批量编辑、批量删除、刷新、导入邮箱。

### 6.2 邮箱表格

列为：选择、邮箱、分组、状态、套餐类型、AT、SK、启用、更新时间、操作。

- AT、SK 只显示是否存在或按需查看入口。
- 启用状态应可快速识别，不建议在表格中误触式直接切换。
- 行操作：查询邮件、编辑、删除。
- 支持状态排序、分页、空状态、加载状态和跨页批量选择。

### 6.3 邮箱编辑

通用字段：邮箱、邮箱类型、渠道、分组、注册状态、套餐类型、是否启用、0 元试用资格。

Microsoft 邮箱字段：邮箱密码、`client_id`、`refresh_token`。标准导入顺序是：

```text
邮箱----密码----client_id----refresh_token
```

导入端会识别常见字段错序并自动规范化，UI 应显示规范格式但不暗示只能接受一种历史数据顺序。

iCloud `xbovo` 字段：邮箱、查询 Key。

iCloud `url_api` 字段：ChatGPT 密码、收码 URL、Base32 TOTP 密钥，并展示各凭证当前是否已配置。它支持：

```text
邮箱
邮箱----密码
邮箱----收码URL
邮箱----密码----2FA密钥
邮箱----密码----收码URL
邮箱----密码----收码URL----2FA密钥
邮箱----收码URL----2FA密钥
```

识别规则：第二段为 HTTP/HTTPS 时按收码 URL，否则按 ChatGPT 密码；后续 URL 为备用收码接口，最后非 URL 字段为 TOTP。只有密码、没有收码 URL 时可导入和密码登录，但需要邮件验证码时必须明确提示“未配置收码接口”。

### 6.4 批量编辑

- 显示选中数量。
- 可修改分组、状态、套餐、启用状态和试用资格。
- 每个字段必须允许“保持不变”，避免未触碰的属性被覆盖。

### 6.5 导入弹窗

- 顶部切换 Microsoft / Apple；Apple 继续切换 `xbovo` / `url_api`。
- 支持手动粘贴与 TXT/CSV 文件拖放。
- 实时显示有效行数、错误行数和逐行错误；提交前禁用不合法批次。
- 同批重复邮箱以最后一条为准；导入已有邮箱会更新分组、渠道和登录凭证，但保留注册状态、Session、Token、试用状态和历史时间。界面应在覆盖前说明这一规则。
- 在弹窗内可选择分组，并提供新建、重命名和删除分组；默认分组不可删除，非空分组不可直接删除。

### 6.6 邮件查询

- Microsoft/xbovo：左侧邮件列表，右侧邮件详情，展示发件人、收件人、时间、主题和正文。
- `url_api`：使用受控的安全邮件网页浏览器，支持后退、前进、刷新以及同源 GET 链接和 GET 表单导航。
- 状态包括连接中、等待邮件、空收件箱、解析失败、接口超时和内容加载完成。

## 7. 接码配置

页面分为“外部供应商配置”和“自建手机号池”。供应商不是装饰性卡片集合，应使用紧凑、可比较的配置区块；每个供应商均有启停、凭证、选项、连通性结果和保存状态。

### 7.1 LubanSMS

- API Key。
- 供应商编号/Service ID。
- Base URL。
- 连接检测、保存、启停。

### 7.2 SMSBower

- API Key、默认国家、默认服务、单号最高价格、Base URL。
- 余额检测、保存、启停。

### 7.3 SMSPool

- API Key、默认国家、默认服务、单号最高价格、Base URL。
- 获取国家/服务选项，选项可搜索。
- 余额检测、保存、启停。

### 7.4 FireFox

- 使用 API 密钥 Token，不是登录密码。
- 默认国家、默认服务、单号最高价格、Base URL。
- 获取真实国家/服务列表，选项可搜索。
- 余额检测、保存、启停。
- 任务侧每 5 秒轮询验证码，不可用号码在 35 秒后释放；设计只需清楚表达供应商状态与配置，不在页面暴露实现噪声。

### 7.5 自建手机号池

- 页面级总开关，控制任务是否分配池中号码。
- 筛选：手机号搜索、状态、使用次数。
- 命令：选择全部筛选结果、批量删除、刷新、导入。
- 表格列：选择、手机号、状态、已用次数、收码 URL、最近使用、操作。
- 手机号和 URL 支持单击复制，并提供成功反馈。
- 行操作：编辑、删除。
- 编辑字段：手机号、状态、成功使用次数、收码 URL。
- 导入支持手动和文件方式，格式为：

```text
+手机号----https://收码链接
```

- 展示格式校验、有效与错误数量。手机号必须为完整 E.164 格式。

## 8. 反代配置

反代页面是单一 Sub2API 集成配置，不需要数据表。

- 总开关：关闭后保留配置但停止自动导入。
- Base URL。
- Admin Token，密码输入样式。
- 账号名称前缀。
- 目标分组：远端多选，可搜索；显示 `ID + 名称`，支持拉取最新选项。
- 远端代理：单选，可选择不使用代理，显示 `ID + 名称`。
- 并发数。
- 负载因子。
- 优先级。
- 模型白名单：多行输入，一行或逗号分隔一个模型；空值表示优先沿用 Session 映射，否则使用默认映射。
- “获取”同时拉取远端分组和远端代理。
- “检测”检查 Base URL、Token 和远端选项，结果就近展示成功或失败原因。
- “保存”仅在内容变更时可用；保存中禁用重复操作。

建议用两个紧凑分区组织“连接凭证”和“导入策略”，底部固定检测与保存操作。不要将每个字段做成独立卡片。

## 9. 代理配置

### 9.1 总览与控制

- 注册流量代理总开关，文案区分开启和关闭后的实际影响。
- 主操作“新增代理”。
- 统计：代理总数、启用、停用、失效；点击统计可快速筛选。
- 筛选：代理地址搜索、状态、国家、最近检测时间排序。
- 批量命令：选择全部筛选结果、批量编辑、批量删除、刷新、批量检测。

代理检测用于判断出口连通性，不应在界面暗示会直接访问 ChatGPT 官网。

### 9.2 代理表格

列为：选择、代理地址、国家、状态、上次检测、操作。

- 地址下方可显示最近错误，需截断但允许查看完整内容。
- 状态显示启用、停用、失效；可用时显示延迟毫秒数。
- 行操作：检测、编辑、删除。
- 支持分页、全筛选选择、加载、空状态和列宽调整。

### 9.3 新增与编辑

- 新增时地址输入为多行，可一次录入多个代理；编辑时为单行。
- 支持 HTTP/HTTPS/SOCKS 等后端允许的代理格式，Placeholder 应给出示例。
- 字段：代理地址、国家、状态。
- 批量编辑可设置国家和状态，国家留空表示保持原值。

## 10. 账户管理

账户管理是信息密度最高的页面，应优先支持扫描、对比和批量维护。

### 10.1 筛选与批量命令

- 筛选：邮箱搜索、分组、状态、套餐类型、试用资格。
- 批量命令：停止 AT 续期、导入反代、试用检测、AT 检测/续期、测活、订阅检测、刷新、定时任务设置。
- 未选择账户时依赖选择的命令应禁用，并用 Tooltip 说明；“停止 AT 续期”和“刷新”等全局命令不依赖选择。
- 命令较多，推荐将高频主命令直接显示，将低频命令收纳到有文字标签的更多菜单；危险和停止操作不可与普通操作混色。

### 10.2 账户表格

列顺序必须保持：

1. 选择。
2. 邮箱。
3. 分组。
4. 状态。
5. 套餐类型。
6. 试用资格。
7. SK。
8. AT。
9. RT。
10. AT 过期时间。
11. 最近测活时间。
12. 操作。

行为要求：

- SK/AT/RT 存在时显示紧凑按钮，按需加载并复制；不存在时为 `-`。
- 没有 RT 时显示“获取”。若账户尚未完成接码，应立即停止流程并提示“当前账户未接码，请先完成接码后再获取 RT”。
- AT 无效、续期失败、探测失败和测活失败显示可点击异常状态，打开完整错误详情。
- AT 过期时间和最近测活时间可排序。
- 试用资格只对“已注册且套餐为 free”的账户显示：绿色“有0元试用”、红色“无0元试用”、未知为 `-`；其他状态或套餐统一为 `-`。
- 行操作：查询邮件、编辑、导出、试用检测、导入反代、AT 续期、测活、订阅检测、删除。
- 行操作必须紧凑且可调列宽。建议保留 1-2 个上下文主操作，其余放入“更多”菜单，但任何命令都不可丢失。

### 10.3 账户编辑与导出

- 编辑字段：分组、状态、套餐、试用资格、AT、RT；敏感值遵循保持/替换/清除语义。
- 导出需明确选择内容：邮箱账户、Auth Session、AT、RT、SK 或全部信息。
- 导出前显示 Token 预览或字段范围，但避免直接在普通页面暴露完整值。

### 10.4 维护任务

- 试用检测支持单条和批量；不符合“已注册 + free”的账户不可检测。
- 检测发现 AT 过期时会启动项目原有 AT 续期任务。
- AT 检测/续期显示有效、无效、失败和已启动续期的数量，并允许停止等待中和执行中的续期任务。
- 测活结果区分存活、封禁和失败。
- 订阅检测更新套餐信息，并有单条/批量运行状态。
- 反代导入展示选中、提交、确认成功、失败、跳过数量及逐账号原因；缺少 AT 或 RT 的账户只跳过自身，不阻断批次。
- 续期任务可在表格下方显示持续进度行，页面刷新后仍可理解当前状态。
- 定时任务弹窗包含“账户定时测活”和“AT 定时检测”，每项有启停、执行时间、频率（小时），并提示配置何时生效。

## 11. 日志管理

### 11.1 页头与统计

- 标题、说明和日志保留策略，保留天数选项为 1/3/7/14/30 天。
- 统计：总日志、今日日志、失败日志、系统日志。

### 11.2 筛选与操作

- 全文搜索。
- 类型、分类、动作、操作者、IP、状态、级别、来源、实体类型。
- 起止时间、Task ID、Request ID。
- 重置筛选。
- 选择全部筛选结果、清除选择。
- 异步导出 CSV/TXT，并持续显示导出进度。
- 删除选中或删除当前筛选结果，必须明确范围并确认。

筛选很多，桌面端可采用两行查询构建器或“常用筛选 + 更多筛选”展开区，不能让 10 余个等宽输入挤成难以扫描的一堵控件。

### 11.3 日志表格与详情

列为：选择、时间、类型/分类、动作/来源、操作者/IP、实体/任务、结果、摘要、详情。

- 结果同时表达成功/失败/警告、HTTP 状态和耗时。
- 摘要截断，完整内容进入详情。
- 操作列在横向滚动时保持可见，同时仍允许用户调整其宽度。
- 详情弹窗显示：日志 ID、时间、操作者、行为、目标实体、任务 ID、请求 ID 和格式化 JSON 详情。
- 每页数量支持 10/20/50/100。

## 12. 数据表统一规范

所有主要列表必须共享一致的数据表行为：

- 表头全选使用清晰的原生或设计系统 Checkbox，包含未选、部分选择、全选、禁用状态；点击区域至少 32px，不使用难辨认的小方框。
- “表头全选当前页”和“选择全部筛选结果”是两个不同动作，必须用选择摘要明确范围。
- 支持横向滚动，表头与数据列保持对齐，固定格式列不可因加载文案产生位移。
- 用户可拖拽每列右边界调整宽度；操作列也必须可调。
- 调宽手柄 Hover/Focus 可见，命中区不小于 8px；拖动时有垂直指示线。
- 双击手柄恢复默认宽度；聚焦手柄后左右键微调，Home 恢复默认。
- 列宽按表格标识持久化到 `localStorage`。
- 支持排序的列显示明确方向，未排序和已排序不能只靠微弱色差。
- 单元格长文本默认省略并通过 Title、Tooltip 或详情查看；邮箱、URL、错误信息不可撑坏列宽。
- 操作列允许换行或更多菜单，但不能产生左右大块无效空白。
- 表格行目标高度约 52-60px；审计日志可略高，以容纳两行信息。
- 每张表都需设计：加载、空数据、筛选无结果、正常、部分选择、全选、行任务运行、行错误、分页边界。

## 13. 组件与交互规范

### 13.1 表单控件

- 常规高度 38-42px，移动端触控高度至少 44px。
- 边框半径建议 6-8px，模态与大面板不超过 8px；避免当前 18-34px 的巨型圆角扩散到运营后台。
- Select 使用自定义可搜索列表时，支持键盘导航、Esc、空结果和长选项截断。
- 二元设置使用 Switch；模式选择使用分段控件或单选卡；多选分组使用带 Checkbox 的搜索下拉。
- 数值使用 Number Input 或 Stepper，并明确最小、最大和单位。
- API 密钥和密码提供显隐按钮，但默认隐藏。
- 所有字段具备标签、帮助信息、错误信息、禁用和只读状态。

### 13.2 按钮

- 主按钮：每个表单或页面最多一个明显主操作。
- 次按钮：检测、刷新、取消等使用中性描边或幽灵样式。
- 危险按钮：删除、停止使用红色语义，并配图标和明确文案。
- 工具类按钮优先使用 Lucide 图标；不熟悉的纯图标必须有 Tooltip。
- 同一区域按钮高度、图标尺寸和间距统一；运行态宽度尽量稳定，避免文案变化造成布局跳动。

### 13.3 状态体系

| 语义 | 用途 | 推荐色彩 |
| --- | --- | --- |
| 成功/可用 | 已注册、已接码、存活、试用可用、检测通过 | Emerald/Green |
| 信息/运行中 | 刷新、导入中、续期中、普通链接 | Sky/Blue |
| 等待/部分完成 | 待注册、未知、限流、阶段未完成 | Amber |
| 错误/危险 | 失败、封禁、Token 无效、删除、停止 | Red |
| 中性/停用 | 未配置、停用、无数据、`-` | Gray |

状态不能只靠颜色；Badge 或文本应包含标签，图标用于加强而非替代含义。大量 Badge 不宜全部使用胶囊，短状态可以使用小圆角标签。

### 13.4 模态、抽屉和确认

- 创建/编辑表单使用居中模态，宽度根据内容约 560-900px，最大高度受视口约束，正文独立滚动。
- 复杂查看场景（邮件、日志详情）桌面端可用宽模态或右侧详情抽屉。
- 移动端模态改为全屏或底部抽屉，页头和主操作保持可见。
- 关闭按钮使用 `X` 图标并有可访问名称；支持 Esc 和合理的焦点圈定。
- 未保存变更关闭时应确认；纯查看弹窗可直接关闭。

## 14. 视觉系统

### 14.1 当前品牌基线

现状以深青背景、Emerald 主色和 Sky 辅色构成，具有半透明玻璃、网格和光晕。核心 Token 如下：

| Token | 深色 | 浅色 |
| --- | --- | --- |
| 页面背景 | `#071318` | `#effdf8` |
| 外壳 | `rgba(8,22,29,.82)` | `rgba(245,255,251,.82)` |
| 卡片 | `rgba(14,35,44,.72)` | `rgba(255,255,255,.82)` |
| 输入背景 | `rgba(6,20,27,.72)` | `rgba(255,255,255,.88)` |
| 主文字 | `#ecfeff` | `#08342f` |
| 次文字 | `#a7f3d0` | `#21655d` |
| 弱文字 | `#7aa5a5` | `#6b8f89` |
| 品牌主色 | `#10b981` | `#10b981` |
| 品牌辅色 | `#38bdf8` | `#0ea5e9` |

字体栈：`Inter, system-ui, Segoe UI, PingFang SC, Microsoft YaHei, Arial, sans-serif`；日志、Token 与 JSON 使用等宽字体。

### 14.2 重设计方向

- 保留绿色作为品牌识别，但用白、近黑和中性灰承载大部分后台界面；Sky 只作为信息辅助色。
- 降低全页面青绿色玻璃感，避免单一色系。语义色必须清晰区分信息、警告、错误和成功。
- 移除背景中的装饰性光球、过强径向渐变和大面积网格；背景应稳定、安静，不干扰数据扫描。
- 登录前页面可以保留少量品牌氛围；管理端采用实色或低透明表面。
- 卡片只用于统计项、重复资源或真正需要边界的工具，不把每个页面区块都画成悬浮卡。
- 不做卡片嵌套；配置页优先分区、分隔线与紧凑表单网格。
- 标题字距为 0，不使用负字距；页面标题约 24-30px，面板标题约 16-20px，表格与表单约 12-14px。
- 边框和阴影克制：1px 中性边框，阴影只用于浮层、吸顶层级和可交互悬浮。

## 15. 响应式设计

现有样式在约 1380、1100、900、760/720、680/640、520/480px 调整布局。新设计不必逐像素沿用断点，但必须覆盖以下行为。

### 15.1 宽桌面（>= 1380px）

- 使用接近全宽的内容区，宽表格完整展开。
- 筛选和命令尽量同一行，但命令过多时允许分为明确的两组。
- 供应商配置可使用 2-4 列表单网格。

### 15.2 窄桌面与平板（900-1379px）

- 工具栏筛选和操作分成两行，操作右对齐或按命令组排列。
- 统计卡从 8 列降为 4 列或 2 列。
- 供应商配置网格降为 2 列，操作独占一行。
- 日志筛选从 5 列降为 3 列。
- 表格保留横向滚动和可调列宽，不强行压缩所有列。

### 15.3 手机（< 720px）

- 顶部导航折叠；内容边距约 10-14px。
- 筛选占满一行，主要命令固定或靠近工具栏，批量低频命令进入菜单。
- 统计项最多 2 列，窄屏可 1 列。
- 表单和模式选择变为单列；供应商操作按钮允许换行但不溢出。
- 登录抽屉和复杂模态全屏化。
- 宽表格不能简单缩小字号。推荐将每行转换为关键信息列表：首行邮箱/号码/地址与状态，次行关键属性，操作进入底部动作栏或更多菜单；提供“查看全部字段”的详情抽屉。
- 若保留横向表格，则要明确提示可横滑并保持选择与操作可达，不能同时冻结过多列。

所有固定格式组件需设置稳定尺寸，避免 Spinner、长英文、Badge 或加载文本引发行高和布局跳动。

## 16. 无障碍与键盘操作

- 正文与背景对比度至少满足 WCAG AA；弱文字不能承担关键状态。
- 所有交互元素有清晰 Focus Ring，不因视觉重设计移除轮廓。
- 图标按钮具备 `aria-label` 和 Tooltip；Switch 具备名称与开关状态。
- 表格列宽手柄可聚焦和键盘调整；排序表头暴露排序状态。
- Checkbox 支持 indeterminate，标签说明当前页或全部筛选范围。
- 模态打开后焦点进入弹窗并被限制在弹窗内，关闭后返回触发元素。
- 错误信息与对应字段关联；Toast 同时进入合适的 Live Region。
- 支持 `prefers-reduced-motion`，关闭非必要平移、缩放、自动滚动动画。
- 中英文切换后不可发生按钮文字裁切、列头遮挡或控件溢出。

## 17. 动效原则

- 页面切换可使用 120-220ms 的淡入和轻微位移。
- 下拉、抽屉、模态使用简短进入/退出反馈，不使用弹跳或大幅缩放。
- 任务进度和状态更新平滑但不延迟真实数据；日志新增不应持续闪烁。
- Hover 只表达可交互性，不让统计卡大幅上浮。
- 加载 Spinner、进度条和骨架用于真实等待；不要添加循环装饰动画。

## 18. Stitch 重设计强制约束

1. 必须设计实际可用的管理端首屏，不以营销 Landing Page 代替后台。
2. 不得删除任何页面、字段、筛选、批量命令、行命令或任务状态。
3. 操作过多时可以重组为主操作与更多菜单，但需要在标注中列出完整命令映射。
4. 所有主要表格必须包含全选、跨页选择、分页、排序、横向滚动和可拖拽列宽；操作列同样可拖拽。
5. 必须同时给出浅色、深色方案，并展示中英文长文案适配。
6. 必须提供桌面 1440px、平板 1024px、手机 390px 的关键页面方案。
7. 必须展示正常、加载、空、筛选无结果、成功、部分完成、失败、禁用、批量选择和运行中状态。
8. 不得用巨型圆角、卡片套卡片、渐变光球或过度玻璃效果掩盖信息层级。
9. 不得直接展示敏感凭证；需设计“已配置”“按需查看/复制”“保持/替换/清除”交互。
10. 设计稿中的控件、表格、弹窗和状态应能映射回 React/Tailwind 组件，而不是只可作为静态概念图。

## 19. Stitch 应交付的画面清单

### 19.1 桌面页面

- 登录前页面 + 登录抽屉（默认、错误、提交中）。
- 管理端全局导航（浅色、深色、当前菜单、移动菜单）。
- 工作台：默认、已选择、注册弹窗、任务运行、部分失败。
- 邮箱配置：列表、编辑、批量编辑、导入、邮件查询、url_api 邮件浏览器。
- 接码配置：四个外部供应商、自建手机号池、导入与编辑。
- 反代配置：未配置、已配置、检测成功、检测失败、远端选项打开。
- 代理配置：列表、新增多行、编辑、批量编辑、检测结果。
- 账户管理：宽表格、更多操作、Token 按需查看、维护任务进度、失败详情、定时任务。
- 日志管理：完整筛选、批量选择、导出进度、详情 JSON、删除确认。

### 19.2 组件与状态板

- 颜色、排版、间距、边框、阴影 Token。
- Button、Icon Button、Input、Password、Textarea、Select、Multi-select、Switch、Stepper。
- Checkbox 的未选/部分/全选/禁用状态。
- Badge、错误状态、Toast、Tooltip、Confirm Bubble。
- 表格表头、排序、列宽手柄、冻结操作列、分页和空状态。
- Modal、Drawer、Tabs、Progress、日志行和任务阶段。

### 19.3 交互标注

- 批量选择范围和跨页行为。
- 表格列宽拖拽、双击恢复、键盘调整与持久化。
- 敏感字段查看、复制、替换与清除。
- 长任务启动、恢复、停止、部分完成和失败详情。
- 桌面表格如何转换为移动端信息列表/详情抽屉。
- 所有被收纳到更多菜单中的原始命令映射。

## 20. 验收标准

Stitch 输出可进入实现阶段的最低标准：

- 七个管理菜单与登录前页面均有明确布局方案。
- 所有业务字段和命令能在设计稿或交互标注中找到。
- 信息密度适合后台高频操作，用户无需经过多层卡片或多次跳转完成常见任务。
- 宽表格、长日志、批量任务、敏感凭证和错误详情均有可执行设计。
- 桌面、平板、手机均无文字遮挡、按钮溢出、控件重叠或不可达操作。
- 主题、语言、键盘、减少动效和语义状态均有一致方案。
- 视觉上仍能识别 SunnyRegister 的绿色品牌，但整体更安静、中性、专业和可扫描。
